import { useLocalStorage } from './useLocalStorage';

export default function App() {
  /*********************  hook usage **************************/

  return (
    <div className='App'>
      <p>use local storage hook的使用</p>
    </div>
  );
}
